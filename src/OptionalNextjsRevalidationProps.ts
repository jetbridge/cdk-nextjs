// ~~ Generated by projen. To modify, edit .projenrc.ts and run "npx projen".
import type { NextjsBuild, NextjsRevalidationOverrides, NextjsServer } from '../';
import type { aws_lambda } from 'aws-cdk-lib';

/**
 * OptionalNextjsRevalidationProps
 */
export interface OptionalNextjsRevalidationProps {
  /**
   * Override props for every construct.
   * @stability stable
   */
  readonly overrides?: NextjsRevalidationOverrides;
  /**
   * Override function properties.
   * @stability stable
   */
  readonly lambdaOptions?: aws_lambda.FunctionOptions;
  /**
   * @stability stable
   */
  readonly serverFunction?: NextjsServer;
  /**
   * @stability stable
   */
  readonly nextBuild?: NextjsBuild;
}
